apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cinemaabyss-ingress
  namespace: cinemaabyss
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
    - host: cinemaabyss.example.com
      http:
        paths:
          - path: /api/events #ивенты идут к ивентам
            pathType: Prefix
            backend:
              service:
                name: events-service
                port:
                  number: 8082
          - path: / #все остальное идет к прокси
            pathType: Prefix
            backend:
              service:
                name: proxy-service
                port:
                  number: 8000